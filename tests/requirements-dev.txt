# DGRA/GPA Development Dependencies
# Install: pip install -r tests/requirements-dev.txt

# Core testing framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0

# Mocking
unittest.mock  # stdlib, listed for clarity
aioresponses>=0.7.6   # aiohttp mocking for API tests
responses>=0.25.0     # requests mocking (if any sync HTTP)

# Coverage reporting
coverage>=7.4.0

# Optional performance/memory profiling
psutil>=5.9.0         # For memory tests in L4
memray>=1.12.0        # Optional: memory leak detection

# Type checking (optional but recommended)
# mypy>=1.8.0
# types-PyYAML

# Linting (optional)
# ruff>=0.2.0
# black>=24.0.0
